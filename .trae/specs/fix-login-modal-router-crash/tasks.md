# Tasks

- [x] 任务 1：稳定并行路由槽位
  - [x] 确认根布局对并行槽位（例如 `modal`）的渲染是可选且具备默认页，避免路由树出现 `undefined` 槽位
  - [x] 确认 `@modal/default` 存在且返回空内容

- [x] 任务 2：修正拦截路由层级与入口导航
  - [x] 明确站内进入登录的入口（例如 `router.push('/login')`）在不同路由深度下的拦截策略
  - [x] 若从深层路由进入 `/login` 无法稳定拦截，调整拦截路由写法（例如从 `(.)login` 改为 `(..)login` 或提供多层覆盖）以确保路由树补丁一致

- [x] 任务 3：将模态交互拆分为“服务端页 + 客户端弹窗组件”
  - [x] 页面层仅负责输出稳定组件树（避免页面级 client 组件在热更新中影响路由树补丁）
  - [x] 客户端组件负责 `open/onOpenChange` 与关闭后的回退逻辑

- [x] 任务 4：关闭与回退策略兜底
  - [x] 关闭模态时优先 `router.back()` 返回来源页
  - [x] 在无历史可回退的情况下提供兜底跳转（例如回首页或固定回退页），避免出现“回退到非预期树”导致异常

- [x] 任务 5：改为状态驱动登录模态（规避拦截路由崩溃）
  - [x] 将站内“去登录”入口改为直接打开 `Dialog`，不再 `router.push('/login')`
  - [x] 在 `AppShell` 内渲染登录弹窗并复用 `LoginView`
  - [x] 登录成功后自动关闭弹窗（例如监听 session/uid 变化）

- [x] 任务 6：验证与回归
  - [x] 开发态：冷启动后，反复打开/关闭登录弹窗并触发 Fast Refresh，不出现 `initialTree is not iterable`
  - [x] 直接访问 `/login` 仍为整页且功能正常
  - [x] 生产态：构建后（或生产模式）导航行为一致，不出现运行时错误

# Task Dependencies

- 任务 2 依赖 任务 1
- 任务 4 依赖 任务 2、任务 3
- 任务 6 依赖 任务 5
